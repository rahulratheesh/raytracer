#include "geometry.h"

#include <iostream>
using namespace std;

int main() {
    Vector3D p(1, 0, 0);
    Vector3D q(0, 1, 0);
    Vector3D r(0, 0, 0);
    //r = p / q;
    //r.println();

    return 0;
}
